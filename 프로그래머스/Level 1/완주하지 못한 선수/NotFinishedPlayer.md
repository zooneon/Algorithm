### ❓ 문제
![image](https://user-images.githubusercontent.com/59433441/125723833-66ebe123-51c1-479b-90cf-4a313412e6a9.png)

### 💬 입출력 예
![image](https://user-images.githubusercontent.com/59433441/125723910-1b2412ad-8401-46ef-9f8f-af02ac911984.png)

### 🤓 풀이
HashMap을 이용하여 풀었다.<br/>
참가자들을 한명씩 HashMap에 넣고, 동명이인이 있을 수 있으므로 `getOrDefault` 메서드를 사용하여 확인한다.<br/>
HashMap에 다 넣었다면 이제 HashMap에서 완주자들을 찾는다.<br/>
모든 작업이 끝난 후에 만약 HashMap의 value가 1인 key는 완주하지 못한 참가자이므로 이 key가 정답이 된다.<br/>
여기서 HashMap의 value를 탐색할 때 keySet을 가져온 후 get(key) 메서드를 사용하지 않고 entrySet을 가져와서 한번에 value를 가져올 수 있도록 하였다.<br/>
그 이유는 keySet을 가져온 다음 get 메서드를 이용하여 값을 가져올 경우 상수시간에 해결하지만 탐색하면서 hashCode, equals 메서드를 계속 호출하기 때문에 성능상 효율적이지 못하다.([링크](https://stackoverflow.com/questions/3870064/performance-considerations-for-keyset-and-entryset-of-map))<br/>
따라서 `entrySet` 메서드로 key와 value를 모두 가져온 후 getKey(), getValue() 메서드를 호출하여 값을 가져오도록 하였다.<br/>
